// Maximum number of items that can be pinned in the sidebar
export const MAX_PINNED_ITEMS = 10;

// Maximum number of recent items to track
export const MAX_RECENT_ITEMS = 10;

// Icons map for categories
export const CATEGORY_ICONS = {
  resources: 'folder-open',
  notebooks: 'book',
  api: 'code',
  database: 'database',
  iam: 'user-shield',
  far: 'tasks',
  plugins: 'puzzle-piece'
};

// Animation classes
export const ANIMATIONS = {
  fadeIn: 'animate-fadeIn',
  slideIn: 'animate-slideIn',
  bounce: 'animate-bounce'
};
